/*-----------------------------------------------------------------------------------------
Макрос  проверки  категория вида хранения
05-02-2012 Жаворонкова Н. (joy) C-17206

Задается в настройках категории 1010 "Вид хранения в ЭА"

Запрещает смену (но не удаление) вида хранения

------------------------------------------------------------------------------------------*/
 
macro CheckAttribute(ObjectType : integer, GroupID : integer, AttrID : integer)
debugbreak;
    msgbox("Для смены вида хранения, пожалуйста, используйте Ctrl+Z на документе" );
    return false;
end;
