import rsd;

macro workbegin()
   var rs,cmd;

   //возврат пользователя из закрытых в открытые

   var oper= 0;
    getint(oper,"Введите номер пользователя");
   if (oper != " ")

      cmd = RSDCommand("update dperson_dbt set T_USERCLOSED=chr(0) where T_OPER="+Oper);
      cmd.Execute();
   end;
end;
workbegin();
msgbox("Выполнено успешно-проверьте!");

