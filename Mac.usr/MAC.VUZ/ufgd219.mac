/*SDA - заглушка для форматов с ошибками          */
/*Chesnokov D.S. 10.04.2012 Проверка на вхождение */
/*      пользователя в группу 198. Заявка С-9530  */
import BankInter, "diver.mac";

macro GenDoc( addrMes )

  if (not ВходитВГруппу({oper},198))
    msgBox("Вы не включены в группу \"198 - Генерация сообщений УФЭБС\"");
    return false;
  end;
  
  return true;
end;

macro GenDocV2( addrMes )
  return GenDoc( addrMes );
end;
