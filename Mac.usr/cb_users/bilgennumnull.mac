/*
  Поиск и генерация номеров с/ф, в том случае если по какой-то причине в номере chr(1).
  Макрос вызывается из ББ->Счета-фактуры->Проверить номера с/ф
  (c) Diver
*/

import "diver.mac";

if (not (ИсправитьРеференсы))
  msgbox("С/Ф с пустым номером обнаружено не было");
  println("Ошибок обнаружено не было");
end; 
exit(1);