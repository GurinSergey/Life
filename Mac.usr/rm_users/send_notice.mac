//Gurin S. C-17745 06.03.2013 add Protocol()
import "send_vk.mac";
import "print_vk.mac";

ПечатьВсехИзвещений(true);
if (gettrue(true,"Отправить Извещения?"))
   ОтправкаВсехИзвещений;
   Protocol("Отправка извещений в Интербанк (средства от нерезидентов)");
   //msgbox("Извещения отправлены");
end;

exit(1);