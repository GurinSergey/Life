/*Str!!! Оизменение шифра операции */


Macro ShifrOper(paymID,SO)
	//var SO=0;

	var cmd, str; 

	if(GetStringR(SO,"Шифр операции",2))

                str = " update dpmrmprop_dbt set T_SHIFROPER='"+so+"' where T_PAYMENTID=" +paymid;
                cmd  = RSDCommand( str );

		cmd.execute;
//		SetParm(1, SO);
	end;
End;//macro